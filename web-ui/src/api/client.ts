const backend=import.meta.env.VITE_API_BASE_URL||'http://localhost:8081'; const llm=import.meta.env.VITE_LLM_HOST_URL||'http://localhost:8000';
export class ApiError extends Error {constructor(message:string,public status:number){super(message)}}
const sessionKey='smart-stock-session-token';let sessionPromise:Promise<string>|null=null;
async function issueSession():Promise<string>{let response:Response;try{response=await fetch(llm+'/api/session',{method:'POST',headers:{Accept:'application/json'}})}catch{throw new ApiError('AI servisine şu anda ulaşılamıyor. Lütfen kısa bir süre sonra tekrar deneyin.',0)}if(!response.ok)throw new ApiError('Güvenli oturum oluşturulamadı.',response.status);const body=await response.json() as {token?:string};if(!body.token)throw new ApiError('Güvenli oturum yanıtı geçersiz.',500);localStorage.setItem(sessionKey,body.token);return body.token}
async function sessionToken():Promise<string>{const existing=localStorage.getItem(sessionKey);if(existing)return existing;if(!sessionPromise)sessionPromise=issueSession().finally(()=>{sessionPromise=null});return sessionPromise}
async function request<T>(url:string,options?:RequestInit):Promise<T>{const isLlm=url.startsWith(llm);const headers:Record<string,string>={'Content-Type':'application/json'};if(isLlm)headers.Authorization=`Bearer ${await sessionToken()}`;if(options?.headers)Object.assign(headers,options.headers);let response:Response;try{response=await fetch(url,{...options,headers})}catch{throw new ApiError('AI servisine şu anda ulaşılamıyor. Lütfen kısa bir süre sonra tekrar deneyin.',0)}if(response.status===401&&isLlm)localStorage.removeItem(sessionKey);if(!response.ok){let message=response.status===401?'Oturum süresi doldu. Lütfen isteği tekrar deneyin.':'İstek tamamlanamadı.';try{const body=await response.json() as {detail?:string};message=body.detail||message}catch{/* response is not JSON */}throw new ApiError(message,response.status)}if(response.status===204)return undefined as T;return response.json() as Promise<T>}
export const api={get:<T>(path:string)=>request<T>(backend+path),post:<T>(path:string,data?:unknown)=>request<T>(backend+path,{method:'POST',body:JSON.stringify(data??{})}),llmGet:<T>(path:string)=>request<T>(llm+path),llmPost:<T>(path:string,data?:unknown)=>request<T>(llm+path,{method:'POST',body:JSON.stringify(data??{})}),llmDelete:<T>(path:string)=>request<T>(llm+path,{method:'DELETE'})};
