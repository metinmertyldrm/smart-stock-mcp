import {useQuery} from '@tanstack/react-query';import {endpoints,keys} from '../api/queries';import {currency,date} from '../utils/format';import {Empty,ErrorState,Loading} from '../components/States';
export function OrdersPage(){const incoming=useQuery({queryKey:keys.incoming,queryFn:endpoints.incoming});const market=useQuery({queryKey:keys.marketOrders,queryFn:endpoints.marketOrders});return <div className="space-y-8"><OrderSection title="Marketplace satın alma siparişleri" loading={market.isLoading} error={market.isError}>{market.data?.length?market.data.map(o=><article className="card" key={o.id}><div className="flex justify-between"><b>Sipariş #{o.id}</b><span className="badge bg-blue-100 text-blue-700">{o.status}</span></div><p className="mt-2 text-sm text-slate-500">{o.items.map(i=>`${i.product.name} (${i.quantity})`).join(', ')}</p><div className="mt-3 flex justify-between text-sm"><span>{date(o.expectedDeliveryDate)}</span><b>{currency(o.totalCost)}</b></div></article>):<Empty title="Marketplace siparişi yok"/>}</OrderSection><OrderSection title="Depoya gelen ikmal siparişleri" loading={incoming.isLoading} error={incoming.isError}>{incoming.data?.length?incoming.data.map(o=><article className="card" key={o.id}><div className="flex justify-between"><b>İkmal #{o.id} · {o.product.name}</b><span className="badge bg-violet-100 text-violet-700">{o.status}</span></div><div className="mt-3 flex justify-between text-sm text-slate-500"><span>{o.quantity} adet</span><span>{date(o.expectedDeliveryDate)}</span></div></article>):<Empty title="Gelen ikmal siparişi yok"/>}</OrderSection></div>}
function OrderSection({title,loading,error,children}:{title:string;loading:boolean;error:boolean;children:React.ReactNode}){return <section><h2 className="mb-4 text-lg font-bold">{title}</h2>{loading?<Loading/>:error?<ErrorState message="Sipariş verileri alınamadı."/>:<div className="grid gap-4 lg:grid-cols-2">{children}</div>}</section>}
